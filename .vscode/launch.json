{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenMP",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/genetic.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "externalConsole": true,
            "preLaunchTask": "Build OpenMP"
        }
    ]
}